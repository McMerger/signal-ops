{
  "name": "signal-ops-root",
  "private": true,
  "scripts": {
    "dev": "docker compose up",
    "stop": "docker compose down",
    "clean": "docker compose down -v",
    "dev:python": "cd workers/strategy-engine && npx wrangler dev",
    "test": "echo 'Run tests in specific service folders'"
  },
  "devDependencies": {
    "baseline-browser-mapping": "^2.8.32"
  }
}