# Core dependencies (Supported by Cloudflare Pyodide)
numpy>=1.24.0
pandas>=2.0.0
# requests is replaced by Pyodide fetch in some envs but kept for compatibility via 'pyodide-http' or native support
requests>=2.31.0

# AI / Research Core
openai>=1.0.0
# google-genai removed (Kimi usage)

# Data fetching
yfinance>=0.2.28
beautifulsoup4>=4.12.0

# No Flask (Worker is stateless/event-driven)
# No SQL/Redis drivers (Use D1/KV bindings if needed, or HTTP APIs)
